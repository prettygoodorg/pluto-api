apiVersion: v1
kind: Service
metadata:
  name: dev-api-service
  namespace: pluto
spec:
  type: ClusterIP
  selector:
    app: dev-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
